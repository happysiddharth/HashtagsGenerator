import 'package:flutter/material.dart';

class FloationActionBtn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      child: Icon(Icons.camera_enhance),
    );
  }
}
